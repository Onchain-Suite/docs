---
title: "Overview"
description: "Understand how Onchain Suite is organized before you send your first campaign."
---

## Two ways in

There are two starting points, depending on who you are.

<CardGroup cols={2}>
  <Card title="Building a dApp" icon="code" href="/api/overview">
    Drop the SDK into your frontend for wallet-based in-app notifications, then send events and read data as you grow. Start with **Build on the Platform**.
  </Card>
  <Card title="Running campaigns" icon="paper-plane" href="/campaigns/overview">
    Set up a workspace, authenticate a domain, build an audience, and send. Start with the operator sections here.
  </Card>
</CardGroup>

Many teams do both: a frontend developer wires up the SDK, and a growth team runs campaigns and automations on top of the same audience.

## Platform Model

Onchain Suite is designed around what growth teams and dApp developers actually need to do, not around backend services or internal data systems.

An operator team typically works in this order:

1. Create a workspace for your team.
2. Configure a project for a brand, product line, or environment.
3. Verify senders and domains.
4. Connect contract, wallet, and product data sources.
5. Review audiences, segments, or the first cohort insight.
6. Launch a campaign or automation.

A dApp developer usually starts the other end: add the [in-app SDK](/integrations/in-app-notifications), send [custom events](/integrations/custom-events), and let automations react to them.

## Core Concepts

### Workspace

A workspace is the top-level home for your organization, billing, and shared access.

### Project

A project contains the settings, senders, integrations, audiences, campaigns, and automations for one operating context.

### Wallet-First Identity

The wallet is the canonical user record. Other identifiers such as email or chat handles attach when they are available, but a wallet does not need a linked email before it becomes usable inside the platform.

### Audience

Audiences can come from imported users, behavioral events, wallet attributes, contract activity, or SQL-generated segments.

### Plays

Plays are pre-built automation patterns for common Web3 lifecycle moments such as first deposit, governance reminders, churn win-back, and migration alerts.

## First-Mile Goal

The best first experience is not a big setup project. It is a quick, valuable insight or a small working workflow that proves your data, targeting, and messaging loop are connected.
